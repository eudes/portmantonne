{
  "name": "portmantonne",
  "version": "0.0.1",
  "description": "",
  "keywords": [
    "portmanteau",
    "generator",
    "portmantonne"
  ],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build --public-url ./ --dist-dir docs index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@eudes/pronounceable": "^2.0.2"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.5.0",
    "@parcel/transformer-webmanifest": "^2.5.0",
    "jest": "^27.5.1",
    "parcel": "^2.5.0"
  }
}
